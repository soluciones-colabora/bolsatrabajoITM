import { MatchValidatorDirective } from './match-email.directive';

describe('MatchValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MatchValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
